<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.example.newsapp.viewmodel.BottomNavigationViewModel" />
    </data>

    <FrameLayout
        android:id="@+id/customBottomNav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="50dp"
        android:layout_marginStart="30dp"
        android:layout_marginEnd="30dp"
        android:elevation="5dp"
        android:layout_alignParentBottom="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintStart_toStartOf="parent"
        android:background="@drawable/bottomnav_bg">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <!-- Item Home -->
            <LinearLayout
                android:id="@+id/itemHome"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                style="@style/CustomLinearLayoutStyle"
                android:layout_weight="1"
                android:orientation="vertical">

                <ImageView
                    android:id="@+id/ivHome"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    app:tint="@color/light_gray"
                    android:src="@drawable/ic_fluent_home_24_regular" />

                <TextView
                    android:id="@+id/textHome"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@{viewModel.selectedItem == `Home` ? @color/light_red : @color/light_gray}"
                    android:text="Home" />

            </LinearLayout>
            <!-- Item Fav -->
            <LinearLayout
                android:id="@+id/itemFav"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                style="@style/CustomLinearLayoutStyle"
                android:layout_weight="1"
                android:orientation="vertical">

                <ImageView
                    android:id="@+id/ivFav"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    app:tint="@color/light_gray"
                    android:src="@drawable/ic_fluent_heart_24_regular" />

                <TextView
                    android:id="@+id/textFav"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@{viewModel.selectedItem == `Favorite` ? @color/light_red : @color/light_gray}"
                    android:text="Favorite" />

            </LinearLayout>
            <!-- Item 3 -->
            <LinearLayout
                android:id="@+id/itemProfile"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                style="@style/CustomLinearLayoutStyle"
                android:layout_weight="1"
                android:orientation="vertical">

                <ImageView
                    android:id="@+id/ivProfile"
                    android:layout_width="24dp"
                    android:layout_height="24dp"
                    app:tint="@color/light_gray"
                    android:src="@drawable/ic_fluent_person_24_regular" />

                <TextView
                    android:id="@+id/textProfile"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:textColor="@{viewModel.selectedItem == `Profile` ? @color/light_red : @color/light_gray}"
                    android:text="Profile"/>

            </LinearLayout>
        </LinearLayout>

    </FrameLayout>
</layout>